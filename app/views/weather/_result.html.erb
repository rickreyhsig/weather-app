<% if result.present? && !result.is_a?(String) %>
  <h2>Weather Data:</h2>
  <pre><%= JSON.pretty_generate(result) %></pre>
<% elsif result.present? %>
  <p style="color: red;"><%= result %></p>
<% else %>
  <p>Enter a city or zip to get the forecast.</p>
<% end %>
